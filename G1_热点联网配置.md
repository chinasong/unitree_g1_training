# G1 热点 + 互联网接入配置指南

## 🌐 网络拓扑

```
┌─────────────────────────────────────────────────┐
│                  互联网                          │
└──────────────────┬──────────────────────────────┘
                   │
            ┌──────┴──────┐
            │   路由器     │
            │ 192.168.1.1  │
            └──────┬───────┘
                   │ 网线
                   │
        ┌──────────┴───────────────────┐
        │      G1 机器人 (Ubuntu)       │
        │                              │
        │  eth0: 192.168.1.100  ◄──────┤ 以太网口（连互联网）
        │  wlan0: 192.168.42.1  ◄──────┤ WiFi热点（给你连接）
        │                              │
        └──────────┬───────────────────┘
                   │ WiFi热点
                   │ "G1_Robot_SONG"
                   │
        ┌──────────┴───────────┐
        │    你的电脑/手机      │
        │  192.168.42.x         │
        │                       │
        │  ✅ 能控制G1          │
        │  ✅ 能访问互联网       │
        └───────────────────────┘
```

## 🚀 快速配置（推荐方法）

### 方法1：使用 NetworkManager（最简单）

NetworkManager会自动配置NAT转发，无需手动设置！

```bash
# 1. 用网线连接G1到路由器（eth0接口）
# 2. SSH登录到G1
ssh unitree@192.168.1.100

# 3. 创建热点（自动开启NAT）
sudo nmcli device wifi hotspot \
    ifname wlan0 \
    ssid "G1_Robot_SONG" \
    password "song123456"

# 4. 设置开机自启
sudo nmcli connection modify Hotspot connection.autoconnect yes

# 完成！NetworkManager已自动配置网络共享
```

**验证互联网连接：**
```bash
# 在连接到G1热点的电脑上
ping 8.8.8.8
curl https://www.baidu.com

# 应该能正常访问
```

### 方法2：手动配置 NAT（高级）

如果NetworkManager没有自动配置，手动设置：

#### 1. 启用IP转发

```bash
# 临时启用
sudo sysctl -w net.ipv4.ip_forward=1

# 永久启用
sudo nano /etc/sysctl.conf
# 添加或取消注释：
net.ipv4.ip_forward=1

# 应用配置
sudo sysctl -p
```

#### 2. 配置 iptables NAT规则

```bash
# 清除现有规则
sudo iptables -t nat -F
sudo iptables -F

# 添加NAT转发规则
sudo iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE

# 允许转发
sudo iptables -A FORWARD -i wlan0 -o eth0 -j ACCEPT
sudo iptables -A FORWARD -i eth0 -o wlan0 -m state --state RELATED,ESTABLISHED -j ACCEPT

# 查看规则
sudo iptables -t nat -L -n -v
sudo iptables -L FORWARD -n -v
```

#### 3. 保存 iptables 规则

```bash
# 安装 iptables-persistent
sudo apt install iptables-persistent

# 保存规则
sudo netfilter-persistent save

# 或手动保存
sudo iptables-save | sudo tee /etc/iptables/rules.v4
```

#### 4. 配置 dnsmasq（提供DNS服务）

如果使用hostapd方式创建热点，需要配置DNS：

```bash
sudo nano /etc/dnsmasq.conf
```

添加：
```conf
interface=wlan0
dhcp-range=192.168.42.10,192.168.42.50,255.255.255.0,24h

# DNS服务器
dhcp-option=6,8.8.8.8,8.8.4.4

# 网关
dhcp-option=3,192.168.42.1
```

重启服务：
```bash
sudo systemctl restart dnsmasq
```

## 📝 完整自动化脚本

创建 `setup_hotspot_with_internet.sh`:

```bash
#!/bin/bash

# G1 WiFi热点 + 互联网共享 一键配置脚本

echo "🌐 G1机器人热点+互联网共享配置"
echo "================================="

# 检查权限
if [ "$EUID" -ne 0 ]; then 
    echo "❌ 请使用sudo运行此脚本"
    exit 1
fi

# 配置参数
WIFI_INTERFACE="wlan0"
ETH_INTERFACE="eth0"
SSID="G1_Robot_SONG"
PASSWORD="song123456"
HOTSPOT_IP="192.168.42.1"

echo ""
echo "📋 网络配置:"
echo "  WiFi接口: $WIFI_INTERFACE (热点)"
echo "  以太网接口: $ETH_INTERFACE (互联网)"
echo "  热点名称: $SSID"
echo "  热点IP: $HOTSPOT_IP"
echo ""

# 检查以太网连接
echo "🔍 检查互联网连接..."
if ! ping -c 1 -W 2 8.8.8.8 > /dev/null 2>&1; then
    echo "⚠️  警告: 无法连接到互联网"
    echo "   请确保 $ETH_INTERFACE 已连接到路由器"
    read -p "继续配置? (y/n) " -n 1 -r
    echo
    if [[ ! $REPLY =~ ^[Yy]$ ]]; then
        exit 1
    fi
else
    echo "✅ 互联网连接正常"
fi

# 启用IP转发
echo "🔧 启用IP转发..."
sysctl -w net.ipv4.ip_forward=1

# 永久启用IP转发
if ! grep -q "^net.ipv4.ip_forward=1" /etc/sysctl.conf; then
    echo "net.ipv4.ip_forward=1" >> /etc/sysctl.conf
fi

# 创建热点
echo "📡 创建WiFi热点..."
nmcli connection delete Hotspot 2>/dev/null || true
nmcli device wifi hotspot ifname $WIFI_INTERFACE ssid "$SSID" password "$PASSWORD"

# 等待热点启动
sleep 2

# 配置NAT
echo "🌐 配置NAT转发..."
iptables -t nat -F
iptables -t nat -A POSTROUTING -o $ETH_INTERFACE -j MASQUERADE
iptables -A FORWARD -i $WIFI_INTERFACE -o $ETH_INTERFACE -j ACCEPT
iptables -A FORWARD -i $ETH_INTERFACE -o $WIFI_INTERFACE -m state --state RELATED,ESTABLISHED -j ACCEPT

# 保存iptables规则
echo "💾 保存防火墙规则..."
if command -v netfilter-persistent &> /dev/null; then
    netfilter-persistent save
else
    # 安装 iptables-persistent
    echo "安装 iptables-persistent..."
    DEBIAN_FRONTEND=noninteractive apt-get install -y iptables-persistent
fi

# 配置NetworkManager共享
echo "⚙️  配置网络共享..."
nmcli connection modify Hotspot connection.autoconnect yes
nmcli connection modify Hotspot ipv4.method shared

# 重启热点使配置生效
echo "🔄 重启热点..."
nmcli connection down Hotspot
nmcli connection up Hotspot

# 验证配置
echo ""
echo "✅ 配置完成！"
echo ""
echo "📊 网络状态:"
nmcli device status
echo ""
echo "🔧 IP转发: $(cat /proc/sys/net/ipv4/ip_forward)"
echo ""
echo "📱 连接信息:"
echo "  WiFi名称: $SSID"
echo "  WiFi密码: $PASSWORD"
echo "  G1 IP: $HOTSPOT_IP"
echo ""
echo "🧪 测试命令:"
echo "  在连接到热点的设备上运行:"
echo "  ping 8.8.8.8"
echo "  curl https://www.baidu.com"
echo ""
echo "💻 启动TTS服务:"
echo "  python3 g1_tts_service.py --iface <WiFi接口> --server"
echo ""
```

使用方法：

```bash
# 在G1机器人上
chmod +x setup_hotspot_with_internet.sh
sudo ./setup_hotspot_with_internet.sh
```

## 🧪 测试验证

### 1. 在G1上验证

```bash
# 检查网络接口
ip addr show

# 应该看到:
# eth0: 192.168.1.100 (连接路由器)
# wlan0: 192.168.42.1 (热点IP)

# 检查互联网
ping -c 3 8.8.8.8

# 检查IP转发
cat /proc/sys/net/ipv4/ip_forward
# 应该输出: 1

# 检查NAT规则
sudo iptables -t nat -L -n -v
```

### 2. 在你的电脑上验证

```bash
# 1. 连接到 G1_Robot_SONG WiFi

# 2. 检查IP地址
ifconfig  # macOS
ip addr   # Linux

# 应该获得: 192.168.42.x

# 3. 测试到G1的连接
ping 192.168.42.1

# 4. 测试互联网
ping 8.8.8.8
ping www.baidu.com

# 5. 测试DNS解析
nslookup www.google.com

# 6. 测试HTTP访问
curl https://www.baidu.com
```

### 3. 测试TTS服务

```bash
# 启动服务
python3 g1_tts_service.py --iface en0 --server

# 测试说话功能
curl -X POST http://localhost:8080/speak \
     -H "Content-Type: application/json" \
     -d '{"text": "互联网连接测试成功"}'

# 在浏览器访问
http://localhost:8080
```

## 🔍 故障排查

### 问题1：能连热点但无法上网

```bash
# 在G1上检查
# 1. IP转发是否启用
cat /proc/sys/net/ipv4/ip_forward
# 如果是0，执行:
sudo sysctl -w net.ipv4.ip_forward=1

# 2. 检查NAT规则
sudo iptables -t nat -L -n -v
# 应该看到 MASQUERADE 规则

# 3. 检查路由
ip route show

# 4. 重新添加NAT规则
sudo iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
```

### 问题2：DNS解析失败

```bash
# 在你的电脑上
# 手动设置DNS服务器
# macOS: 系统偏好设置 -> 网络 -> 高级 -> DNS
# 添加: 8.8.8.8, 8.8.4.4

# Linux:
sudo nano /etc/resolv.conf
# 添加:
nameserver 8.8.8.8
nameserver 8.8.4.4
```

### 问题3：部分网站无法访问

```bash
# 可能是MTU问题
# 在G1上设置
sudo ip link set dev wlan0 mtu 1400

# 或在NetworkManager中设置
sudo nmcli connection modify Hotspot 802-3-ethernet.mtu 1400
```

### 问题4：网速很慢

```bash
# 检查WiFi信道
iwconfig wlan0

# 切换到不拥挤的信道
sudo nmcli connection modify Hotspot 802-11-wireless.channel 6

# 使用5GHz频段（如果支持）
sudo nmcli connection modify Hotspot 802-11-wireless.band a
```

## 📊 性能优化

### 1. 使用5GHz频段

```bash
# 如果WiFi卡支持5GHz
sudo nmcli connection modify Hotspot 802-11-wireless.band a
sudo nmcli connection modify Hotspot 802-11-wireless.channel 36

# 重启热点
nmcli connection down Hotspot && nmcli connection up Hotspot
```

### 2. 优化TCP参数

```bash
# 在G1上
sudo nano /etc/sysctl.conf

# 添加:
net.ipv4.tcp_congestion_control=bbr
net.core.default_qdisc=fq
net.ipv4.tcp_notsent_lowat=16384

# 应用
sudo sysctl -p
```

### 3. 限制带宽（可选）

```bash
# 安装 tc (traffic control)
sudo apt install iproute2

# 限制热点出口带宽为10Mbps
sudo tc qdisc add dev wlan0 root tbf rate 10mbit burst 32kbit latency 400ms
```

## 🎯 实际应用场景

### 场景1：户外演示

```
G1机器人
├─ 网线连接4G/5G路由器 (互联网)
├─ WiFi热点给观众连接
└─ 观众控制机器人 + 查资料
```

### 场景2：实验室开发

```
G1机器人
├─ 网线连接实验室网络 (互联网)
├─ WiFi热点给开发团队
└─ 团队成员协同开发 + 查文档
```

### 场景3：展会演示

```
G1机器人
├─ 网线连接场馆网络
├─ WiFi热点给访客
└─ 访客体验 + 分享到社交媒体
```

## 💡 安全建议

### 1. 使用强密码

```bash
# 设置复杂密码
sudo nmcli connection modify Hotspot 802-11-wireless-security.psk "YourStrongPassword123!"
```

### 2. 限制连接数量

```bash
# 最多允许10个设备
sudo nmcli connection modify Hotspot 802-11-wireless.max-peers 10
```

### 3. 启用MAC地址过滤

```bash
# 只允许特定设备连接
sudo nmcli connection modify Hotspot 802-11-wireless-security.mac-address-blacklist "AA:BB:CC:DD:EE:FF"
```

### 4. 监控连接设备

```bash
# 查看已连接设备
sudo arp -a | grep 192.168.42

# 实时监控
watch -n 2 'sudo arp -a | grep 192.168.42'

# 踢出特定设备
sudo arp -d 192.168.42.100
```

## 📚 NetworkManager 配置详解

### 查看热点配置

```bash
# 显示完整配置
nmcli connection show Hotspot

# 显示IPv4设置
nmcli -f ipv4 connection show Hotspot

# 显示WiFi设置
nmcli -f 802-11-wireless connection show Hotspot
```

### 修改热点设置

```bash
# 修改SSID
sudo nmcli connection modify Hotspot 802-11-wireless.ssid "New_SSID"

# 修改密码
sudo nmcli connection modify Hotspot 802-11-wireless-security.psk "newpassword"

# 修改IP地址
sudo nmcli connection modify Hotspot ipv4.addresses "192.168.100.1/24"

# 应用修改
nmcli connection down Hotspot && nmcli connection up Hotspot
```

## 🎓 工作原理详解

### NAT转发流程

```
你的电脑 (192.168.42.10)
    ↓
发送请求: 访问 www.baidu.com
    ↓
通过网关 192.168.42.1 (G1 wlan0)
    ↓
G1 NAT转换: 
  源IP: 192.168.42.10 → 192.168.1.100 (G1 eth0)
    ↓
通过 eth0 发送到路由器
    ↓
路由器转发到互联网
    ↓
收到响应
    ↓
G1 NAT反向转换:
  目标IP: 192.168.1.100 → 192.168.42.10
    ↓
通过 wlan0 转发给你的电脑
    ↓
你的电脑收到网页内容
```

### iptables 规则说明

```bash
# 1. MASQUERADE规则
# 将从wlan0来的数据包的源IP改为eth0的IP
iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE

# 2. FORWARD规则
# 允许从wlan0到eth0的转发
iptables -A FORWARD -i wlan0 -o eth0 -j ACCEPT

# 3. 允许已建立连接的返回流量
iptables -A FORWARD -i eth0 -o wlan0 -m state --state RELATED,ESTABLISHED -j ACCEPT
```

## 🚀 快速命令参考

```bash
# === G1机器人端配置 ===

# 一键创建热点+互联网共享
sudo nmcli device wifi hotspot ifname wlan0 ssid "G1_Robot_SONG" password "song123456"
sudo sysctl -w net.ipv4.ip_forward=1
sudo iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE

# 检查状态
nmcli device status
cat /proc/sys/net/ipv4/ip_forward
sudo iptables -t nat -L -n -v

# 重启热点
nmcli connection down Hotspot && nmcli connection up Hotspot


# === 电脑端使用 ===

# 连接热点
# WiFi: G1_Robot_SONG
# 密码: song123456

# 测试连接
ping 192.168.42.1    # 测试到G1
ping 8.8.8.8         # 测试互联网
curl https://www.baidu.com  # 测试HTTP

# 启动TTS服务
python3 g1_tts_service.py --iface en0 --server
```

## ✅ 总结

使用G1热点 + 互联网共享方案的优势：

| 特性 | 说明 |
|------|------|
| 🎯 **便携性** | 只需一根网线，随时可用 |
| 🌐 **双通道** | 既能控制G1，又能上网 |
| 🚀 **低延迟** | 直连G1，5-15ms |
| 🔒 **独立网络** | 不影响其他设备 |
| 💻 **多设备** | 支持多人同时连接 |
| 📱 **全平台** | 手机/电脑/平板都能用 |

**配置一次，永久享受！** 🎉

