# G1机器人音频设备测试总结

## 测试结果 ✅

### USB音响
- **状态**: ✅ 工作正常
- **设备ID**: `alsa_output.usb-Generic_USB2.0_Device_20170726905923-01.analog-stereo`
- **格式**: 立体声，48kHz
- **测试结果**: ✅ 播放测试成功

### USB麦克风（Type-C转USB）
- **状态**: ✅ 工作正常
- **设备ID**: `alsa_input.usb-Generic_USB2.0_Device_20170726905923-01.mono-fallback`
- **格式**: 单声道，48kHz
- **测试结果**: ✅ 录音测试成功（480KB文件）

## 设备配置

### 默认设备设置
- **默认输出设备**: USB音响（已设置）
- **默认输入设备**: USB麦克风（已设置）
- **音响音量**: 70%
- **麦克风音量**: 100%

### 设备规格
| 设备 | 声道 | 采样率 | 状态 |
|------|------|--------|------|
| USB音响 | 立体声 (2ch) | 48kHz | ✅ 正常 |
| USB麦克风 | 单声道 (1ch) | 48kHz | ✅ 正常 |

## 注意事项

### 格式兼容性
- 麦克风录音是**单声道**，音响播放是**立体声**
- 使用 `paplay` 播放时会自动转换格式
- 直接使用 `aplay` 播放单声道文件会失败

### 推荐播放方式
```bash
# 方式1: 使用PulseAudio播放（推荐，自动格式转换）
paplay /tmp/g1_audio_test.wav

# 方式2: 使用sox转换后播放
sox /tmp/g1_audio_test.wav -r 48000 -c 2 /tmp/stereo.wav
aplay -D hw:0,0 /tmp/stereo.wav
```

## 测试脚本

### 1. 完整音频测试
```bash
./g1_test_audio_full.sh
```
- 配置设备
- 测试录音（5秒）
- 播放录音验证
- 播放测试音

### 2. 简单麦克风测试
```bash
./g1_test_mic_simple.sh
```
- 快速录音测试
- 自动播放验证

### 3. 设备配置
```bash
./g1_setup_audio.sh
```
- 设置USB设备为默认
- 配置音量

## 使用建议

### 在Python程序中使用

```python
import subprocess

# 录音（单声道，48kHz）
subprocess.run([
    'arecord', '-D', 'hw:0,0', '-d', '5',
    '-f', 'S16_LE', '-r', '48000', '-c', '1',
    '-t', 'wav', 'recording.wav'
])

# 播放（使用PulseAudio自动转换）
subprocess.run(['paplay', 'recording.wav'])

# 或播放立体声音频
subprocess.run(['aplay', '-D', 'hw:0,0', 'stereo_audio.wav'])
```

### 在TTS服务中使用

TTS服务已经配置好，可以直接使用：
```bash
python3 g1_tts_service.py --iface wlan0 --server
```

音响会自动播放TTS生成的语音。

## 故障排查

### 如果设备未检测到
1. 检查USB连接
2. 运行 `lsusb` 查看USB设备
3. 检查驱动：`dmesg | tail -20`

### 如果录音失败
1. 检查麦克风权限
2. 确认设备ID正确
3. 使用正确的格式参数（单声道，48kHz）

### 如果播放失败
1. 使用 `paplay` 而不是 `aplay`（自动格式转换）
2. 或先转换格式再播放
3. 检查音量设置

## 总结

✅ **USB音响和麦克风都已正常工作**
✅ **设备已正确配置为默认设备**
✅ **可以用于语音交互和TTS服务**

Type-C麦克风通过USB转接器连接，系统识别为USB设备，这是正常的。

