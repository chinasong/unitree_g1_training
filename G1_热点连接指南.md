# G1 机器人热点连接指南

## 🌐 连接方案：G1开启WiFi热点

### 方案概述

```
┌─────────────────────────────────────────┐
│         G1 机器人 (Ubuntu)               │
│                                         │
│  ┌──────────────────────────────┐      │
│  │   WiFi热点 (AP模式)           │      │
│  │   SSID: G1_Robot_xxxx        │      │
│  │   密码: ********             │      │
│  │   IP: 192.168.42.1           │      │
│  └────────────┬─────────────────┘      │
│               │ WiFi信号                │
└───────────────┼─────────────────────────┘
                │
                │ 连接WiFi
                ↓
        ┌───────────────┐
        │  你的电脑      │
        │  IP: 192.168.42.x │
        └───────────────┘
```

**优势：**
- ✅ 无需路由器
- ✅ 直连，低延迟
- ✅ 便携，随时可用
- ✅ 网络配置简单
- ✅ 机器人和电脑独立网络

## 📋 在G1机器人上配置WiFi热点

### 方法1：使用 NetworkManager (推荐)

#### 1. SSH登录到G1机器人

```bash
# 首先通过有线连接或现有网络SSH到G1
ssh unitree@<G1的IP地址>
# 默认密码通常是: 123
```

#### 2. 检查网络接口

```bash
# 查看可用的WiFi接口
ip link show
# 或
nmcli device status

# 通常WiFi接口名称是：
# - wlan0
# - wlp2s0
# - wlx开头的USB WiFi适配器
```

#### 3. 创建WiFi热点

```bash
# 使用nmcli创建热点
sudo nmcli device wifi hotspot \
    ifname wlan0 \
    ssid "G1_Robot_SONG" \
    password "song123456"

# 参数说明：
# ifname wlan0      - WiFi接口名称
# ssid              - 热点名称（你电脑看到的WiFi名）
# password          - 热点密码（至少8位）
```

#### 4. 查看热点状态

```bash
# 查看热点是否启动
nmcli connection show

# 查看热点详细信息
nmcli device show wlan0

# 应该看到：
# IP地址: 192.168.42.1
# 子网掩码: 255.255.255.0
```

#### 5. 设置开机自启

```bash
# 让热点开机自动启动
sudo nmcli connection modify Hotspot connection.autoconnect yes

# 或者重启验证
sudo nmcli connection up Hotspot
```

### 方法2：使用 hostapd + dnsmasq (高级)

如果NetworkManager不可用，可以使用传统方式：

#### 1. 安装必要软件

```bash
sudo apt update
sudo apt install hostapd dnsmasq
```

#### 2. 配置 hostapd

```bash
# 创建配置文件
sudo nano /etc/hostapd/hostapd.conf
```

添加以下内容：

```conf
# WiFi接口
interface=wlan0

# 驱动
driver=nl80211

# 热点名称
ssid=G1_Robot_SONG

# 频道
channel=6

# 加密方式
hw_mode=g
wpa=2
wpa_passphrase=song123456
wpa_key_mgmt=WPA-PSK
wpa_pairwise=CCMP
rsn_pairwise=CCMP

# 其他设置
macaddr_acl=0
auth_algs=1
ignore_broadcast_ssid=0
```

#### 3. 配置 dnsmasq (DHCP服务器)

```bash
# 备份原配置
sudo mv /etc/dnsmasq.conf /etc/dnsmasq.conf.bak

# 创建新配置
sudo nano /etc/dnsmasq.conf
```

添加以下内容：

```conf
# 监听接口
interface=wlan0

# DHCP地址池
dhcp-range=192.168.42.10,192.168.42.50,255.255.255.0,24h

# 网关和DNS
dhcp-option=3,192.168.42.1
dhcp-option=6,192.168.42.1
```

#### 4. 配置网络接口

```bash
# 设置wlan0静态IP
sudo nano /etc/network/interfaces
```

添加：

```conf
auto wlan0
iface wlan0 inet static
    address 192.168.42.1
    netmask 255.255.255.0
```

或使用netplan (Ubuntu 18.04+):

```bash
sudo nano /etc/netplan/99-wlan0.yaml
```

```yaml
network:
  version: 2
  wifis:
    wlan0:
      dhcp4: no
      addresses: [192.168.42.1/24]
```

#### 5. 启动服务

```bash
# 应用网络配置
sudo netplan apply

# 启动hostapd
sudo systemctl unmask hostapd
sudo systemctl enable hostapd
sudo systemctl start hostapd

# 启动dnsmasq
sudo systemctl enable dnsmasq
sudo systemctl start dnsmasq

# 检查状态
sudo systemctl status hostapd
sudo systemctl status dnsmasq
```

### 方法3：使用 create_ap (最简单)

```bash
# 安装create_ap
git clone https://github.com/oblique/create_ap
cd create_ap
sudo make install

# 创建热点
sudo create_ap wlan0 eth0 G1_Robot_SONG song123456

# 开机自启
sudo systemctl enable create_ap
```

## 💻 在你的电脑上连接

### 步骤1：连接WiFi

**macOS:**
```bash
# 在WiFi列表中找到 "G1_Robot_SONG"
# 点击连接，输入密码: song123456
```

**Linux:**
```bash
# 使用nmcli
nmcli device wifi connect "G1_Robot_SONG" password "song123456"

# 或使用GUI
# 点击WiFi图标 -> 选择 G1_Robot_SONG -> 输入密码
```

**Windows:**
```
1. 点击任务栏WiFi图标
2. 找到 "G1_Robot_SONG"
3. 点击连接，输入密码: song123456
```

### 步骤2：验证连接

```bash
# 检查IP地址（应该是 192.168.42.x）
ifconfig en0  # macOS
ip addr show wlan0  # Linux

# 测试连接
ping 192.168.42.1

# 应该看到：
# 64 bytes from 192.168.42.1: icmp_seq=1 ttl=64 time=2.34 ms
```

### 步骤3：运行TTS服务

```bash
# 查看你电脑的WiFi接口
ifconfig  # macOS
ip addr   # Linux

# 启动TTS服务（使用连接到G1热点的接口）
cd /Users/pengs/Downloads/workspace/unitree_g1_training

# macOS
python3 g1_tts_service.py --iface en0 --server

# Linux
python3 g1_tts_service.py --iface wlan0 --server
```

### 步骤4：测试

```bash
# 测试连接
curl -X POST http://localhost:8080/speak \
     -H "Content-Type: application/json" \
     -d '{"text": "你好，我是SONG的机器人"}'

# 或在浏览器打开
http://localhost:8080
```

## 🔧 完整配置脚本

### 一键配置脚本（在G1上运行）

创建文件 `setup_hotspot.sh`:

```bash
#!/bin/bash

# G1 WiFi热点一键配置脚本

echo "🤖 G1机器人WiFi热点配置工具"
echo "================================"

# 检查权限
if [ "$EUID" -ne 0 ]; then 
    echo "❌ 请使用sudo运行此脚本"
    exit 1
fi

# 配置参数
INTERFACE="wlan0"
SSID="G1_Robot_SONG"
PASSWORD="song123456"
IP="192.168.42.1"

echo ""
echo "📋 配置信息:"
echo "  接口: $INTERFACE"
echo "  热点名称: $SSID"
echo "  密码: $PASSWORD"
echo "  IP地址: $IP"
echo ""

read -p "确认开始配置? (y/n) " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    exit 1
fi

echo "🔧 检查NetworkManager..."
if ! command -v nmcli &> /dev/null; then
    echo "❌ NetworkManager未安装"
    echo "正在安装..."
    apt update
    apt install -y network-manager
fi

echo "📡 停止现有连接..."
nmcli connection down id Hotspot 2>/dev/null || true

echo "🌐 创建WiFi热点..."
nmcli connection delete id Hotspot 2>/dev/null || true
nmcli device wifi hotspot ifname $INTERFACE ssid "$SSID" password "$PASSWORD"

echo "⚙️  配置IP地址..."
nmcli connection modify Hotspot ipv4.addresses "$IP/24"
nmcli connection modify Hotspot ipv4.method shared

echo "🚀 启动热点..."
nmcli connection up id Hotspot

echo "✅ 设置开机自启..."
nmcli connection modify Hotspot connection.autoconnect yes

echo ""
echo "✅ 配置完成！"
echo ""
echo "📱 连接信息:"
echo "  WiFi名称: $SSID"
echo "  密码: $PASSWORD"
echo "  机器人IP: $IP"
echo ""
echo "💻 在你的电脑上:"
echo "  1. 连接WiFi: $SSID"
echo "  2. 运行命令: python3 g1_tts_service.py --iface <WiFi接口> --server"
echo ""
echo "🧪 测试连接: ping $IP"
echo ""
```

使用方法：

```bash
# 在G1机器人上
chmod +x setup_hotspot.sh
sudo ./setup_hotspot.sh
```

## 🔍 故障排查

### 问题1：热点无法启动

```bash
# 检查WiFi接口状态
nmcli device status

# 如果接口被占用，先断开
sudo nmcli device disconnect wlan0

# 重新创建热点
sudo nmcli device wifi hotspot ifname wlan0 ssid "G1_Robot_SONG" password "song123456"
```

### 问题2：电脑无法获取IP

```bash
# 在G1上检查DHCP服务
sudo systemctl status NetworkManager

# 重启NetworkManager
sudo systemctl restart NetworkManager

# 手动重启热点
nmcli connection down Hotspot
nmcli connection up Hotspot
```

### 问题3：连接上但无法通信

```bash
# 在G1上检查IP转发
sudo sysctl -w net.ipv4.ip_forward=1

# 检查防火墙
sudo ufw status
sudo ufw allow from 192.168.42.0/24

# 检查iptables
sudo iptables -L -n -v
```

### 问题4：DDS通信失败

```bash
# 检查端口是否开放
sudo netstat -tulpn | grep 7400

# 允许DDS端口
sudo ufw allow 7400/udp
sudo ufw allow 7401/udp

# 禁用IPv6（如果有问题）
sudo sysctl -w net.ipv6.conf.all.disable_ipv6=1
```

## 📊 网络拓扑对比

### 方案A：通过路由器

```
电脑 ──WiFi──▶ 路由器 ◀──网线── G1
           (中转)
延迟: ~20-50ms
需要: 路由器
```

### 方案B：网线直连

```
电脑 ◀──网线──▶ G1
    (直连)
延迟: ~2-10ms
需要: 网线
```

### 方案C：G1热点（推荐！）

```
电脑 ──WiFi──▶ G1 (热点)
    (直连)
延迟: ~5-15ms
需要: 仅需WiFi
```

## 🎯 完整使用流程

### 1. 在G1上配置热点（一次性）

```bash
# SSH到G1
ssh unitree@<G1当前IP>

# 运行配置脚本
sudo nmcli device wifi hotspot \
    ifname wlan0 \
    ssid "G1_Robot_SONG" \
    password "song123456"

# 设置开机自启
sudo nmcli connection modify Hotspot connection.autoconnect yes
```

### 2. 电脑连接G1热点

```bash
# macOS/Linux: 在WiFi列表选择 "G1_Robot_SONG"
# 密码: song123456

# 验证连接
ping 192.168.42.1
```

### 3. 启动TTS服务

```bash
cd /Users/pengs/Downloads/workspace/unitree_g1_training

# 使用WiFi接口（连接到G1热点的接口）
python3 g1_tts_service.py --iface en0 --server

# 应该看到:
# 🤖 初始化G1机器人连接...
# 📢 当前音量: 80
# ✅ G1机器人连接成功！
```

### 4. 测试功能

```bash
# Web界面
http://localhost:8080

# 命令行测试
curl -X POST http://localhost:8080/speak \
     -H "Content-Type: application/json" \
     -d '{"text": "热点连接测试成功"}'
```

## 💡 高级配置

### 隐藏SSID（增强安全性）

```bash
# 创建隐藏热点
sudo nmcli connection modify Hotspot 802-11-wireless.hidden yes
```

### 限制连接数量

```bash
# 最多允许5个设备连接
sudo nmcli connection modify Hotspot 802-11-wireless.max-peers 5
```

### 设置5GHz频段（更快速度）

```bash
# 如果WiFi支持5GHz
sudo nmcli connection modify Hotspot 802-11-wireless.band a
sudo nmcli connection modify Hotspot 802-11-wireless.channel 36
```

### 查看已连接设备

```bash
# 查看连接到热点的设备
sudo arp -a | grep 192.168.42

# 或
sudo nmap -sn 192.168.42.0/24
```

## 📚 参考命令速查

```bash
# === G1机器人端 ===

# 创建热点
sudo nmcli device wifi hotspot ifname wlan0 ssid "G1_Robot_SONG" password "song123456"

# 查看状态
nmcli connection show
nmcli device status

# 重启热点
nmcli connection down Hotspot && nmcli connection up Hotspot

# 删除热点
nmcli connection delete Hotspot


# === 电脑端 ===

# 连接热点
nmcli device wifi connect "G1_Robot_SONG" password "song123456"

# 查看IP
ifconfig en0  # macOS
ip addr show wlan0  # Linux

# 测试连接
ping 192.168.42.1

# 启动服务
python3 g1_tts_service.py --iface en0 --server
```

## 🎉 总结

使用G1自带的WiFi热点是**最简单、最便携**的连接方式：

✅ **优点：**
- 无需额外硬件
- 配置简单
- 随时随地可用
- 低延迟直连
- 独立网络环境

⚠️ **注意事项：**
- 确保G1的WiFi功能正常
- 热点开启后G1无法同时连接其他WiFi
- 信号范围约10-30米
- 如需联网可通过G1的以太网口

**现在就可以让G1开启热点，直接连接使用了！** 🚀

