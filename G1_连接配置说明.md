# G1 机器人 TTS 服务 - 连接配置详解

## 🔌 连接原理

### 核心连接机制

程序通过 **DDS (Data Distribution Service)** 协议与G1机器人通信，使用 **CycloneDDS** 作为底层通信库。

```
┌─────────────┐                    ┌─────────────┐
│  你的电脑    │                    │  G1 机器人   │
│             │                    │             │
│  TTS服务    │◄──── 局域网 ────►│  音频服务    │
│  (Python)   │     (DDS/UDP)     │  (C++)      │
│             │                    │             │
└─────────────┘                    └─────────────┘
```

## 📡 连接步骤详解

### 1. 网络接口初始化

```python
# 第31行：关键连接代码
ChannelFactoryInitialize(0, network_interface)
```

**参数说明：**
- `0`: 域ID（Domain ID），用于隔离不同的DDS网络
- `network_interface`: 网络接口名称，如 `eth0`、`en0`、`enp3s0`

**作用：**
- 指定使用哪个网络接口与机器人通信
- 初始化DDS通信工厂
- 建立底层UDP多播连接

### 2. 创建音频客户端

```python
# 第33-35行
self.audio_client = AudioClient()
self.audio_client.SetTimeout(10.0)
self.audio_client.Init()
```

**流程：**
1. 创建 `AudioClient` 实例
2. 设置10秒超时（等待机器人响应的最长时间）
3. 初始化客户端，注册TTS、音量、LED等API

### 3. 验证连接

```python
# 第38-42行
ret, volume_data = self.audio_client.GetVolume()
if ret == 0 and volume_data:
    current_volume = volume_data.get('volume', 50)
    print(f"📢 当前音量: {current_volume}")
```

**验证方式：**
- 尝试获取机器人当前音量
- 如果成功返回，说明连接建立
- 如果失败，使用默认值继续运行

## 🌐 网络配置要求

### 必要条件

1. **同一局域网**
   ```
   电脑IP:     10.103.3.107
   机器人IP:   10.103.x.x (需在同一网段)
   子网掩码:   255.255.224.0
   ```

2. **网络连接方式**
   - 🔸 **有线连接**（推荐）：网线直连或通过同一交换机
   - 🔸 **WiFi连接**：连接到同一WiFi网络

3. **防火墙设置**
   - 允许UDP多播通信
   - 开放DDS使用的端口（通常是动态端口）

### 网络接口选择

#### macOS 系统
```bash
# 查看所有网络接口
ifconfig

# 常见接口：
# en0  - WiFi（无线网卡）
# en1  - 雷雳网桥
# en2  - 雷雳网桥
# en3  - 以太网（有线网卡）
```

#### Linux 系统
```bash
# 查看所有网络接口
ip addr
# 或
ifconfig

# 常见接口：
# eth0    - 以太网
# wlan0   - WiFi
# enp3s0  - PCI以太网卡
```

## 🔧 配置示例

### 场景1：通过WiFi连接

```bash
# 假设你的电脑和G1都连接到同一WiFi
# macOS使用en0（WiFi接口）
python3 g1_tts_service.py --iface en0 --server

# Linux使用wlan0
python3 g1_tts_service.py --iface wlan0 --server
```

### 场景2：通过有线网卡连接

```bash
# macOS使用有线网卡
python3 g1_tts_service.py --iface en3 --server

# Linux使用以太网
python3 g1_tts_service.py --iface eth0 --server
```

### 场景3：网线直连机器人

```bash
# 1. 设置电脑IP为静态（例如 192.168.123.100）
# 2. 确认机器人IP（通常是 192.168.123.x）
# 3. 使用直连的网络接口
python3 g1_tts_service.py --iface eth0 --server
```

## 🔍 连接问题诊断

### 检查网络连接

```bash
# 1. 查看本机网络接口和IP
ifconfig

# 2. 尝试ping机器人（假设机器人IP是 192.168.123.161）
ping 192.168.123.161

# 3. 检查是否能发现DDS服务
# （需要机器人开机并运行服务）
```

### 常见连接错误

#### 错误1：无法初始化通道
```
错误信息: Failed to initialize channel
原因: 网络接口名称错误或不存在
解决: 使用 ifconfig 确认正确的接口名称
```

#### 错误2：超时无响应
```
错误信息: Timeout
原因: 无法找到机器人服务
解决: 
- 确认机器人已开机
- 检查网络连接
- 确认在同一局域网
- 检查防火墙设置
```

#### 错误3：权限错误
```
错误信息: Permission denied
原因: 没有网络接口访问权限
解决: 在Linux上可能需要 sudo 权限
```

## 🎯 DDS 通信详解

### 什么是DDS？

**DDS (Data Distribution Service)** 是一种实时发布-订阅通信协议：

```
┌─────────────────────────────────────────┐
│           DDS 网络（局域网）              │
│                                         │
│  ┌──────┐    ┌──────┐    ┌──────┐    │
│  │发布者│───▶│ DDS  │◀───│订阅者│    │
│  │(G1)  │    │ Broker│    │(PC)  │    │
│  └──────┘    └──────┘    └──────┘    │
│                                         │
│  • 自动服务发现                          │
│  • UDP多播通信                          │
│  • 低延迟实时传输                        │
└─────────────────────────────────────────┘
```

### TTS服务通信流程

```
1. 初始化阶段：
   PC ──[ChannelFactoryInitialize]──▶ 广播发现请求
   G1 ◀──[Service Discovery]──────── 响应可用服务

2. TTS请求：
   PC ──[TtsMaker("你好")]──▶ G1音频服务
   PC ◀──[Success/Error]───── G1确认收到

3. G1执行：
   G1内部 ──[TTS引擎]──▶ 语音合成
   G1扬声器 ◀───────────  播放音频
```

### API注册机制

```python
# 在AudioClient.Init()中：
self._RegistApi(ROBOT_API_ID_AUDIO_TTS, 0)        # 1001: TTS
self._RegistApi(ROBOT_API_ID_AUDIO_SET_VOLUME, 0) # 1006: 音量
self._RegistApi(ROBOT_API_ID_AUDIO_SET_RGB_LED, 0)# 1010: LED
```

每个API都有唯一的ID，通过DDS Topic进行通信。

## 📝 配置最佳实践

### 1. 确认机器人网络配置

**在G1机器人上查看（如果有SSH访问权限）：**
```bash
# 查看机器人IP地址
ifconfig

# 查看DDS服务是否运行
ps aux | grep dds
```

### 2. 推荐的网络设置

**方案A：路由器组网（推荐）**
```
路由器 (192.168.1.1)
  ├─ 电脑 (192.168.1.100)  ──WiFi──▶ 路由器
  └─ G1   (192.168.1.161)  ──网线──▶ 路由器

连接命令：
python3 g1_tts_service.py --iface en0 --server
```

**方案B：网线直连**
```
电脑 (192.168.123.100) ◀──网线──▶ G1 (192.168.123.161)

步骤：
1. 设置电脑网卡为静态IP: 192.168.123.100
2. G1通常自动配置为: 192.168.123.161
3. 使用有线网口：
   python3 g1_tts_service.py --iface eth0 --server
```

**方案C：WiFi热点**
```
电脑开启热点 (192.168.137.1)
  └─ G1连接到电脑热点 (192.168.137.x)

连接命令：
python3 g1_tts_service.py --iface en0 --server
```

### 3. 验证连接成功

连接成功的标志：
```bash
🤖 初始化G1机器人连接...
📢 当前音量: 80
✅ G1机器人连接成功！

🌐 HTTP服务器启动成功！
📍 本地访问: http://localhost:8080
```

如果看到这些信息，说明：
1. ✅ 网络接口正确
2. ✅ DDS通信建立
3. ✅ 机器人服务发现成功
4. ✅ 可以开始使用TTS功能

## 🚀 快速开始

### 标准流程

```bash
# 1. 查看网络接口
ifconfig | grep -E "^[a-z]" | cut -d: -f1

# 2. 确认连接方式（WiFi/有线）
# WiFi通常是: en0 (macOS) 或 wlan0 (Linux)
# 有线通常是: en3 (macOS) 或 eth0 (Linux)

# 3. 启动服务
python3 g1_tts_service.py --iface <你的网络接口> --server

# 4. 访问Web界面测试
# 打开浏览器: http://localhost:8080
```

### 测试连接

```bash
# 方式1：使用简单测试程序
python3 g1_tts_simple_test.py <网络接口>

# 方式2：使用curl测试
curl -X POST http://localhost:8080/speak \
     -H "Content-Type: application/json" \
     -d '{"text": "连接测试"}'

# 成功响应：
# {"success": true, "message": "语音发送成功"}

# 失败响应（未连接机器人）：
# {"success": false, "message": "语音发送失败，错误码: -1"}
```

## 📚 相关文档

- Unitree SDK文档: https://support.unitree.com
- DDS规范: https://www.omg.org/spec/DDS/
- CycloneDDS: https://github.com/eclipse-cyclonedds/cyclonedds

## ❓ 常见问题

**Q: 为什么需要指定网络接口？**
A: 因为一台电脑可能有多个网络接口（WiFi、有线、虚拟网卡等），DDS需要知道在哪个接口上广播和接收数据。

**Q: 可以同时使用多个接口吗？**
A: 不可以。一个TTS服务实例只能绑定一个网络接口。

**Q: 机器人IP地址会变化吗？**
A: 如果使用DHCP，IP可能会变化。建议配置静态IP或通过MAC地址绑定固定IP。

**Q: 延迟多少？**
A: DDS协议延迟通常在10-50ms之间，取决于网络质量。

**Q: 支持远程连接吗？**
A: 不建议。DDS设计用于局域网，远程连接需要VPN或端口转发，且性能会下降。

---

**现在你理解连接原理了吗？** 🎓

关键就是：
1. **同一局域网** 
2. **正确的网络接口**
3. **DDS服务发现**

