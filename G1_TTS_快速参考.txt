═══════════════════════════════════════════════════════════
    G1 机器人 TTS 服务 - 快速参考卡片
═══════════════════════════════════════════════════════════

📁 创建的文件：
  ✓ g1_tts_service.py          - 主服务程序
  ✓ g1_tts_client_example.py   - Python客户端示例  
  ✓ g1_tts_simple_test.py      - 简单测试程序
  ✓ quick_start_tts.sh         - 快速启动脚本
  ✓ G1_TTS_SERVICE_README.md   - 详细文档
  ✓ G1_TTS_项目说明.md          - 项目总览
  ✓ G1_TTS_快速参考.txt         - 本文件

───────────────────────────────────────────────────────────
🚀 最快上手方式（3步）
───────────────────────────────────────────────────────────

1. 连接G1机器人到网络

2. 运行测试程序：
   $ python3 g1_tts_simple_test.py eth0

3. 看到输出："✅ 所有测试完成！"

───────────────────────────────────────────────────────────
🌐 启动HTTP服务（推荐）
───────────────────────────────────────────────────────────

启动服务：
$ python3 g1_tts_service.py --iface eth0 --server

浏览器访问：
http://localhost:8080

测试API：
$ curl -X POST http://localhost:8080/speak \
       -H "Content-Type: application/json" \
       -d '{"text": "你好，G1！"}'

───────────────────────────────────────────────────────────
💻 交互式模式
───────────────────────────────────────────────────────────

$ python3 g1_tts_service.py --iface eth0 --interactive

>>> 你好，我是G1机器人
>>> /volume 80
>>> /led 255 0 0
>>> /quit

───────────────────────────────────────────────────────────
📡 HTTP API 速查
───────────────────────────────────────────────────────────

让机器人说话：
POST /speak
{"text": "要说的话", "speaker_id": 0}

设置音量：
POST /volume/set  
{"volume": 80}

获取音量：
GET /volume/get

设置LED：
POST /led/set
{"r": 255, "g": 0, "b": 0}

───────────────────────────────────────────────────────────
🐍 Python 代码示例
───────────────────────────────────────────────────────────

# 方式1: 使用客户端类
from g1_tts_client_example import G1TTSClient
client = G1TTSClient("http://localhost:8080")
client.speak("你好")

# 方式2: 直接使用SDK
from unitree_sdk2py.core.channel import ChannelFactoryInitialize
from unitree_sdk2py.g1.audio.g1_audio_client import AudioClient

ChannelFactoryInitialize(0, "eth0")
audio = AudioClient()
audio.SetTimeout(10.0)
audio.Init()
audio.TtsMaker("你好", 0)

───────────────────────────────────────────────────────────
🔧 常用命令
───────────────────────────────────────────────────────────

# 查看网络接口
$ ifconfig

# 快速测试
$ python3 g1_tts_simple_test.py eth0

# 启动服务（默认8080端口）
$ python3 g1_tts_service.py --iface eth0 --server

# 自定义端口
$ python3 g1_tts_service.py --iface eth0 --server --port 9000

# 后台运行
$ nohup python3 g1_tts_service.py --iface eth0 --server > tts.log 2>&1 &

# 停止服务
$ pkill -f g1_tts_service.py

# 查看日志
$ tail -f tts.log

───────────────────────────────────────────────────────────
🌍 其他语言调用示例
───────────────────────────────────────────────────────────

JavaScript/Node.js:
fetch('http://localhost:8080/speak', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({text: '你好'})
});

Bash/Shell:
curl -X POST http://localhost:8080/speak \
     -H "Content-Type: application/json" \
     -d '{"text": "你好"}'

───────────────────────────────────────────────────────────
⚠️  常见问题
───────────────────────────────────────────────────────────

Q: 无法连接机器人？
A: 检查网络接口名称，使用 ifconfig 查看

Q: 机器人不说话？
A: 检查音量设置，尝试 client.set_volume(100)

Q: 端口被占用？
A: 使用 --port 指定其他端口

Q: 权限错误？
A: 确保网络接口配置正确，可能需要sudo权限

───────────────────────────────────────────────────────────
📚 文档位置
───────────────────────────────────────────────────────────

完整文档：G1_TTS_SERVICE_README.md
项目说明：G1_TTS_项目说明.md
示例代码：g1_tts_client_example.py

───────────────────────────────────────────────────────────
🎯 典型使用流程
───────────────────────────────────────────────────────────

开发测试阶段：
1. 运行 g1_tts_simple_test.py 确认连接
2. 启动 HTTP 服务器
3. 用浏览器 Web 界面测试
4. 编写自己的客户端代码

生产部署阶段：
1. 使用 nohup 后台运行服务
2. 配置防火墙规则
3. 设置开机自启（systemd）
4. 监控日志文件

───────────────────────────────────────────────────────────
💡 最佳实践
───────────────────────────────────────────────────────────

✓ 使用HTTP服务器模式（而非直接调用SDK）
✓ 为长文本添加适当标点，提高语音自然度
✓ 控制发送频率，避免命令积压
✓ 生产环境添加错误处理和重连机制
✓ 使用环境变量配置网络接口
✓ 记录日志便于调试

───────────────────────────────────────────────────────────
🚀 立即开始
───────────────────────────────────────────────────────────

最简单的方式：
$ python3 g1_tts_simple_test.py eth0

推荐方式：
$ python3 g1_tts_service.py --iface eth0 --server
然后打开浏览器访问 http://localhost:8080

═══════════════════════════════════════════════════════════
    开始享受G1机器人的语音交互吧！🤖✨
═══════════════════════════════════════════════════════════

